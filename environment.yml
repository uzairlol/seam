name: seam
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - pip
  - pip:
      - ollama>=0.3.0
      - pydantic>=2.0
      - pyyaml>=6.0
      - numpy>=1.26
      - scipy>=1.12
      - pandas>=2.2
      - matplotlib>=3.8
      - seaborn>=0.13
      - sentence-transformers>=2.7
      - nltk>=3.8
      - tqdm>=4.66
      - rich>=13.0
      - jsonlines>=4.0
      - pytest>=8.0
      - pytest-mock>=3.12
      - mypy>=1.10
      - ruff>=0.4
